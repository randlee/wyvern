# Template catalog — format notes (g.6)

Companion to [g6-template-wizard.md](g6-template-wizard.md). Manifest shape, picker `config.templates`, apply path, and overwrite matrix live only in that sprint doc.

Template apply is **only** `workflow.post` / `apply-template.py`. There is no shipped `cp -R` install path.
