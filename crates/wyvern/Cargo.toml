[package]
name             = "wyvern-cli"
version.workspace    = true
edition.workspace    = true
rust-version.workspace = true
license.workspace    = true
repository.workspace = true
homepage.workspace   = true
description  = "What You View, Engine Renders Natively — HTTP dialog CLI"
keywords     = ["wyvern", "dialog", "webview", "cli", "json"]
categories   = ["command-line-utilities", "gui"]
readme       = "../../README.md"
# ui/ is included here for cargo-install users (no separate share/wyvern/ui needed).
# rust-embed maps workspace share/wyvern/** and scripts/ext/** from crate-relative
# ../../ paths (see extensions/mod.rs) so {wyvern_share} resolves in both dev and
# cargo-install layouts. Cargo `include` can only package files under this crate
# directory, so those workspace assets are compiled into the binary rather than
# listed here.
include      = ["src/**", "ui/**", "Cargo.toml"]

[lib]
name = "wyvern"
path = "src/lib.rs"

[[bin]]
name = "wyvern"
path = "src/main.rs"

[dependencies]
wyvern-schema      = { workspace = true }
wyvern-host        = { workspace = true }
serde              = { workspace = true }
serde_json         = { workspace = true }
tracing            = { workspace = true }
tracing-subscriber = { version = "0.3", default-features = false, features = ["fmt", "env-filter", "std"] }
sc-observability   = { workspace = true }
rust-embed         = { version = "8", features = ["compression"] }
dirs               = { workspace = true }
tempfile           = "3"

[dev-dependencies]
reqwest    = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "blocking"] }
serde_json = { workspace = true }
tempfile   = "3"
