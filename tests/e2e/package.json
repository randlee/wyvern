{
  "name": "wyvern-e2e",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "playwright test",
    "test:message": "playwright test message.spec.ts",
    "test:input": "playwright test input.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
