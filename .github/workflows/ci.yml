name: CI

on:
  pull_request:
    branches: [develop, main, "integrate/*"]
  push:
    branches: [develop, main]

jobs:
  build:
    name: Build & Test (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux webview deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          # webkit2gtk for wry; xvfb for headless GTK/winit window tests (phase-A README)
          sudo apt-get install -y libwebkit2gtk-4.1-dev xvfb

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: cargo build
        run: cargo build --workspace

      - name: cargo test
        if: runner.os != 'Linux'
        run: cargo test --workspace

      # GTK/winit need a display; ubuntu-latest is headless — use Xvfb (docs/plans/phase-A/README.md).
      - name: cargo test (Linux / xvfb)
        if: runner.os == 'Linux'
        env:
          GDK_BACKEND: x11
        run: xvfb-run -a cargo test --workspace

      - name: cargo clippy
        run: cargo clippy --workspace -- -D warnings

  fmt:
    name: Format check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  boundaries:
    name: Boundary lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: sc-lint-boundary check
        run: echo "sc-lint-boundary enforcement — wired in Phase 2"
